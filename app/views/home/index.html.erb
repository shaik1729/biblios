<div class="container text-center">
    <div class="row">
        <div class="col">
            <div class="card">
                <div class="card-body">
                  <h5 class="card-title">BIBLIOS is a virtual library for all your need you will get all the documents you need over here.</h5>
                    <% if user_signed_in? %>
                        <p class="card-text">You are logged in as <%= current_user.email %></p>
                    <% else %>
                        <p class="card-text">Sign in / Sign up to get the service</p>
                    <% end %>
                </div>
              </div>
        </div>
    </div>
</div>